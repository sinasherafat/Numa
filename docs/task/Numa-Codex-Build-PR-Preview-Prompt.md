# Build Numa v1.0 GitHub PR and verified Vercel Preview

This is the readable task contract recovered from the current Codex task history after the temporary Pages attachment became unavailable.

Build Numa v1.0 in `sinasherafat/Numa`, preserve all six features, verify the application, push a feature branch, create or update one real GitHub PR, and deliver a Vercel Preview matching its latest commit. Do not merge, enable auto-merge, deploy Production, bypass protections, purchase services, or claim an external action without verifying it.

Use the explicit task instructions first, then confirmed product decisions and `ALA-SPEC-1.0`, then proposed implementation defaults, then five visual mockups. Use Numa in the shell, metadata, and accessible labels. English is the initial product language. Do not make the product Persian-only or connect it to Pangaan.

Keep F01 Adaptive Listening, F02 My Understanding, F03 Explain It Back, F04 Compare Sources, F05 What Changed, and F06 Learn for a Goal in scope. Preserve B01–B13, S01–S15, and J01–J06. Future unplayed chapters may change only after acceptance; past/current audio remains immutable. Memory requires explicit consent and distinguishes exposure, self-report, explain-back evidence, gaps, and not assessed. Explain-back supports voice or text, transcript correction, grounded feedback, cannot-assess, and targeted review. Comparison must show agreements, differences, possible reasons, unknowns, non-comparable conditions, and insufficient evidence. Change review uses an explicit baseline, detects duplicates, separates publication/import/review dates, and updates only on explicit action. Goals change the path and editable outputs, including presentation practice, notes, flashcards, comparison, and textual slide outlines.

Implement one responsive application shell, not screenshot embeds. Match the light, restrained mockups; keep controls readable, navigation route-correct, keyboard accessible, non-autoplaying, and usable at 1440, 768, 390, and 320 pixels. Fix mockup contradictions such as a paused player showing Pause or unassessed evidence implying mastery.

Use server-side ownership, transactional metadata, private object storage, replaceable generation/STT/TTS adapters, immutable revision assets, citation/source alignment, versioned retrieval, and durable idempotent jobs with checkpoints, cancellation, and bounded retry. Never rely on an in-memory queue or Production local disk. Treat sources as untrusted and block private-network URL fetching. Do not leak secrets or private content into clients or analytics.

Provide a clearly labeled, coherent sample workspace. Keep it separate from real private document processing and model evaluation. If live providers are blocked, deploy the sample, keep the PR Draft, list blocked acceptance criteria, and never call the fixture a completed private integration.

Run typecheck, lint, unit/integration tests, production build, and real browser verification. Compare all five reference routes at desktop size and check tablet/mobile/320 overflow. Create traceability, setup, env-name template, architecture decision, migration/runtime instructions, test evidence, and a human review checklist.

Push the existing implementation branch and verify its remote SHA. Create one PR against the actual default branch and update it as fixes land. The PR body must cover all six feature statuses, tests, Preview URL, limitations, and review steps. Keep it Draft while required integration gates are blocked.

Deploy Preview only. Use the existing Vercel project if present; otherwise create/link an authorized project carefully. Verify framework/root/build/runtime, Preview env, hosted routes and flows, logs, and that PR head, remote branch, and deployed source SHA match. Do not promote or deploy Production.

Final handoff must be in Persian and include the actual PR URL/status, actual Preview URL/state/SHA, a six-row feature table, checks and hosted-browser verification, five running-app screenshots, ordered review steps, genuine blockers with one concrete action each, and explicit confirmation that no merge or Production deployment occurred.
